<template>
    <div class="row">
          44444444444
    </div>
</template>

<script>
    export default {
        name: "ActForm",
        data(){
            return {

            }
        },
        mounted(){
            /*

            axios.post('ajax/ingredient',{id:this.id})
                .then(response => {
                    if (response.data.success) {
                        this.ings=response.data.results.ing;
                        response.data.results.ingredients.forEach(el=>{
                            this.counter+=1;
                            this.items.push({
                                id: this.counter,
                                ingredient:el.id,
                                count:el.count
                            })
                        })
                    }
                })
                .catch(function (error) {
                    this.$swal.fire({
                        type: "error",
                        title: "Помилка",
                        timer: 2500
                    });
                })
                .finally(function () {
                });

                */

        },
    }
</script>

<style scoped>

</style>